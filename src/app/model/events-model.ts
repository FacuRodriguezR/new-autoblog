interface EventItem {
    title: string;
    date: string;
    location: string;
    image: string;
}

interface EventsCarousel {
    id: string,
    slug: string,
    title: string,
    image: string,

}
interface BrandsAgreements {
    id: number,
    brand: string,
    image: string,

}

interface Sponsors {
    id: number,
    brand: string,
    image: string
}